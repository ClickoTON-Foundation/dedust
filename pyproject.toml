[project]
name = "dedust"
description = "DeDust - DeDust SDK for Python."
readme = "README.md"
version = "1.0.0"
authors = [
    {name = "Shibdev"},
    {name = "Dalvpv"}
]
dependencies = [
    "aiohttp>=3.8.5",
    "tonsdk>=1.0.13"
]

[build-system]
build-backend = "flit_core.buildapi"
requires = ["flit_core >=3.2,<4"]

